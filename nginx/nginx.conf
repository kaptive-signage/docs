worker_processes  1;

events {
  worker_connections  1024;
}

http {
  # Extract preferred locale from the Accept-Language header
  map $http_accept_language $preferred_locale {
    default  "";
    ~*^fr    "fr";
    ~*^de    "de";
    ~*^it    "it";
  }

  server {
    listen 8080;
    server_name   _;

    root   /usr/share/nginx/html;
    index  index.html index.htm;
    include /etc/nginx/mime.types;

    gzip on;
    gzip_min_length 1000;
    gzip_proxied expired no-cache no-store private auth;
    gzip_types text/plain text/css application/json application/javascript application/x-javascript text/xml application/xml application/xml+rss text/javascript;

    error_page 404 /404.html;
    location = /404.html {
            root /usr/share/nginx/html;
            internal;
    }

    # Static assets: serve directly, no redirect
    location /_astro {
      try_files $uri =404;
    }

    location ~* \.(svg|ico|png|jpg|jpeg|gif|webp|css|js|woff2?|ttf|eot)$ {
      try_files $uri =404;
    }

    # Already-localized paths: serve directly, no redirect
    location ~ ^/(fr|de|it)(/|$) {
      try_files $uri $uri/index.html =404;
    }

    # Paths without a locale prefix: redirect to preferred language if detected
    location / {
      if ($preferred_locale != "") {
        return 302 https://$host/$preferred_locale$request_uri;
      }
      try_files $uri $uri/index.html =404;
    }
  }
}